jwt:
  secret: 'osmantech123'
server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/empl_db
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

